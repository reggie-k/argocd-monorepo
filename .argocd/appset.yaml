apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: monorepo-apps
spec:
  preservedFields:
    annotations: ["codefresh.io/resync"]
  generators:
    - git:
        repoURL: 'https://github.com/reggie-k/argocd-monorepo.git'
        revision: main
        directories:
          - path: 'app-*'
        requeueAfterSeconds: 10
  template:
    metadata:
      name: '{{path.basename}}'
    spec:
      project: default
      source:
        repoURL: 'https://github.com/reggie-k/argocd-monorepo.git'
        targetRevision: main
        path: '{{path}}'
      destination:
        server: 'https://kubernetes.default.svc'
        namespace: '{{path.basename}}'
      syncPolicy:
        syncOptions:
        - CreateNamespace=true
        automated:
          prune: true
          selfHeal: true
